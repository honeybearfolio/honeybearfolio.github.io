{
  "name": "honeybearfolio.github.io",
  "version": "1.0.0",
  "description": "HoneyBear Folio project website built with Hugo and Tailwind CSS",
  "scripts": {
    "dev": "hugo server --buildDrafts --disableFastRender",
    "build": "hugo --minify",
    "build:css": "bunx tailwindcss -i ./assets/css/main.css -o ./public/css/main.css --minify",
    "clean": "rm -rf public resources"
  },
  "keywords": ["hugo", "tailwindcss", "personal-finance"],
  "author": "HoneyBearFolio",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.18",
    "@tailwindcss/typography": "^0.5.19",
    "tailwindcss": "^4.1.18"
  }
}
